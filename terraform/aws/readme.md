# AWS Terraform Configuration

This directory contains Terraform configuration used for validating
AWS infrastructure definitions within CI/CD workflows.

The configuration supports Terraform plan and validation only.
No resources are provisioned.